
<button @click="uploadFile" type="button" for="attachment" class="newPost__option__file__btnInvisible"><i class="bi bi-image"></i></button>
<input type="file" ref="uploadFile" name="attachment" @change="onFileSelected"  accept="image/*" id="attachment" aria-label="Modifier ma photo de profil">






axios.post('http://localhost:3000/api/users/messages/create', postForm,{
                    headers: {
                        'Content-Type': 'application/json',
                       'Authorization': 'Bearer ' + localStorage.getItem('token')
                    },
                })
                .then((res) => {
                    console.log(res.data);
                    // window.location.reload()
                })
                .catch(error => {
                    const msgerror = error.response.data;
                    this.notyf.error(msgerror.error)    
                })